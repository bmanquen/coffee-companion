// Shared by MinutesSecondsInput and SteepMinutesInput. Duration placeholders
// look like filled values ("2 min", "45 sec", "18 hr"), so they use the
// dimmer treatment already used for empty notes — not the default input
// placeholder weight.
export const durationInputClassName = 'placeholder:text-muted-foreground/60'
