# cp packages/api/.env.test.example packages/api/.env.test
#
# Must point at the test-environment database: the integration and e2e suites
# delete and re-insert their test users on every run. This file wins over a
# DATABASE_URL already exported in your shell, which would otherwise name a
# development database.
DATABASE_URL="postgresql://user:password@host:port/database"
