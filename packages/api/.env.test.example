# cp packages/api/.env.test.example packages/api/.env.test
#
# Must point at the test-environment database: the suite deletes and re-inserts
# its test users on every run. This file wins over a DATABASE_URL already
# exported in your shell, which would otherwise name a development database.
DATABASE_URL="postgresql://user:password@host:port/database"
